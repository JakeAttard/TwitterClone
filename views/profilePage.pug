extends layouts/main-layout.pug

block content  
    if !profileUser
        span.errorMessage Error the username requested can not be found
    else
        span #{profileUser.firstName}
        .postsContainer

        +createPostModals(userLoggedIn)

block scripts
    script(src="/js/profile.js")